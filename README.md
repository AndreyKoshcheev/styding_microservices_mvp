# 🚀 Микросервисная система рекомендаций товаров

**Система персонализированных рекомендаций на основе Event Storming архитектуры**

Проект представляет собой полнофункциональную MVP систему рекомендаций товаров, которая анализирует поведение пользователей и генерирует персонализированные предложения на основе коллаборативной фильтрации.

## 📋 Описание проекта

Система предназначена для интернет-магазинов и e-commerce платформ, которым необходима интеллектуальная система рекомендаций. Она отслеживает действия пользователей (просмотры, добавления в корзину, покупки) и на основе этих данных предлагает релевантные товары.

**Ключевые особенности:**
- 🎯 Персонализированные рекомендации в реальном времени
- 🔄 Обновление ML-моделей без остановки сервиса (hot-swap)
- 📊 Event-driven архитектура на основе Event Storming
- 🐳 Полная контейнеризация с Docker
- 📈 Масштабируемая микросервисная архитектура

## 🛠️ Быстрый старт

### Требования
- Docker и Docker Compose
- 4GB RAM минимум
- 2GB свободного дискового пространства

### Установка и запуск

**Способ 1: Docker (рекомендуемый)**

```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd microVIVT

# 2. Настройка окружения (.env уже включен в проект)

# 3. Запуск всех сервисов (Docker автоматически установит зависимости)
docker compose up -d

# 4. Проверка статуса (ожидайте healthy status)
docker compose ps

# 5. Проверка работоспособности
curl http://localhost/health
```

**Способ 2: Локальный запуск (для разработки)**

```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd microVIVT

# 2. Установка зависимостей
npm install

# 3. Настройка окружения (.env уже включен в проект)

# 4. Запуск PostgreSQL и Redis
# (требуется установленный PostgreSQL и Redis)

# 5. Инициализация базы данных
node scripts/init-database.js

# 6. Запуск всех сервисов
npm run dev

# 7. Проверка работоспособности
curl http://localhost:3003/health
```

**Ожидаемый результат:** `Recommendation System is healthy`

> **Примечание:** Docker-способ автоматически устанавливает все зависимости внутри контейнеров. Не нужно предварительно устанавливать Node.js зависимости - они устанавливаются во время сборки Docker образов.

## 🧪 Тестирование системы

### 1. Проверка здоровья системы
```bash
curl http://localhost/health
```
**Что значит:** Система полностью работоспособна

### 2. Отслеживание активности пользователя
```bash
curl -X POST http://localhost/api/v1/activity/track \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "user-1",
    "activityType": "view",
    "productId": "product-1",
    "metadata": {
      "source": "mobile-app",
      "duration": 45
    }
  }'
```
**Что значит:** Пользователь user-1 просмотрел товар product-1 в течение 45 секунд

**Проверка результата:**
```bash
curl "http://localhost/api/v1/recommendations/activity/user-1?limit=1"
```
**Что значит:** Показывает последнюю активность пользователя user-1. Вы должны увидеть только что созданную запись.

### 3. Получение персонализированных рекомендаций
```bash
curl "http://localhost/api/v1/recommendations/recommendations/user-1?limit=5"
```
**Что значит:** Получить 5 персонализированных рекомендаций для пользователя user-1

**Пример ответа:**
```json
{
  "success": true,
  "userId": "user-1",
  "recommendations": [
    {
      "productId": "product-8",
      "score": 2.0,
      "confidence": 1.0,
      "reason": "collaborative_filtering",
      "product": {
        "id": "product-8",
        "name": "Рюкшак для ноутбука",
        "category": "Аксессуары",
        "price": "2499.00"
      }
    }
  ],
  "generatedAt": "2024-01-15T10:30:00.000Z",
  "fromCache": false
}
```

### 4. Добавление товара в корзину
```bash
curl -X POST http://localhost/api/v1/activity/track \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "user-1",
    "activityType": "add_to_cart",
    "productId": "product-2",
    "metadata": {
      "quantity": 2
    }
  }'
```
**Что значит:** Пользователь user-1 добавил 2 единицы товара product-2 в корзину

**Проверка влияния на рекомендации:**
```bash
curl -X POST http://localhost/api/v1/recommendations/recommendations/user-1/refresh \
  -H "Content-Type: application/json" \
  -d '{"limit": 5}'
```
**Что значит:** Принудительно обновить рекомендации для user-1. Новые рекомендации должны учесть только что добавленный товар.

### 5. Покупка товара
```bash
curl -X POST http://localhost/api/v1/activity/track \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "user-1",
    "activityType": "purchase",
    "productId": "product-2",
    "metadata": {
      "quantity": 1,
      "price": 8999.00
    }
  }'
```
**Что значит:** Пользователь user-1 купил товар product-2 за 8999.00

### 6. Статистика рекомендаций
```bash
curl "http://localhost/api/v1/recommendations/stats/user-1?days=7"
```
**Что значит:** Показать статистику рекомендаций для пользователя user-1 за последние 7 дней

### 7. Обучение ML-модели
```bash
curl -X POST http://localhost/api/v1/training/train \
  -H "Content-Type: application/json" \
  -d '{
    "config": {
      "weights": {
        "view": 1.0,
        "add_to_cart": 2.5,
        "purchase": 5.0
      },
      "timeDecay": 0.9
    }
  }'
```
**Что значит:** Запустить обучение новой ML-модели с указанными весами для разных типов активности

## 🏗️ Архитектура микросервисов

### 1. **User Activity Service** (порт 3001)
**Ответственность:** Сбор и анализ действий пользователей
- Отслеживание всех пользовательских активностей
- Сохранение данных в PostgreSQL
- Публикация событий в EventBus
- Автоматическая инициализация базы данных

### 2. **Recommendation Engine** (порт 3002)
**Ответственность:** Генерация персонализированных рекомендаций
- Коллаборативная фильтрация
- Поиск похожих пользователей
- Расчет схожести товаров
- Поддержка hot-swap моделей

### 3. **Recommendation API** (порт 3003)
**Ответственность:** Представление рекомендаций пользователям
- REST API для получения рекомендаций
- Кэширование результатов
- Агрегация данных из разных сервисов
- API для отслеживания активности

### 4. **Model Training Service** (порт 3004)
**Ответственность:** Обновление ML-моделей
- Фоновое обучение моделей
- Валидация качества моделей
- Hot-swap без остановки сервиса
- Очередь обучения моделей

### 5. **Infrastructure Services**
- **PostgreSQL** (порт 5432) - Основное хранилище данных
- **Redis** (порт 6379) - EventBus и кэширование
- **Nginx** (порт 80) - API Gateway

## 📚 Основные команды и запросы

### Управление системой
```bash
# Запуск всех сервисов
docker compose up -d

# Остановка всех сервисов
docker compose down

# Пересборка и запуск
docker compose up -d --build

# Просмотр логов
docker compose logs -f [service-name]

# Проверка статуса контейнеров
docker compose ps
```

### API эндпоинты

**Отслеживание активности:**
- `POST /api/v1/activity/track` - Отследить действие пользователя

**Рекомендации:**
- `GET /api/v1/recommendations/recommendations/{userId}` - Получить рекомендации
- `POST /api/v1/recommendations/recommendations/{userId}/refresh` - Обновить рекомендации
- `GET /api/v1/recommendations/activity/{userId}` - История активности
- `GET /api/v1/recommendations/stats/{userId}` - Статистика рекомендаций

**Обучение моделей:**
- `POST /api/v1/training/train` - Запустить обучение модели
- `GET /api/v1/training/status` - Статус обучения
- `GET /api/v1/training/models` - Список моделей

**Health checks:**
- `GET /health` - Общее здоровье системы
- `GET /api/v1/activity/health` - User Activity Service
- `GET /api/v1/engine/health` - Recommendation Engine
- `GET /api/v1/recommendations/health` - Recommendation API
- `GET /api/v1/training/health` - Model Training Service

## 💻 Технологический стек

**Backend:**
- **Node.js 18** - Runtime среда
- **Express.js** - Веб-фреймворк
- **TypeScript** - Типизация (в планах)

**Базы данных:**
- **PostgreSQL 15** - Основная СУБД
- **Redis 7** - EventBus и кэш

**Машинное обучение:**
- **Коллаборативная фильтрация** - Основной алгоритм
- **Real-time обучение** - Обновление моделей на лету

**Инфраструктура:**
- **Docker & Docker Compose** - Контейнеризация
- **Nginx** - API Gateway и балансировка
- **Event Sourcing** - Архитектурный паттерн

**Event Storming:**
- **Event-driven архитектура** - Событийно-ориентированное взаимодействие
- **Domain Events** - События предметной области
- **CQRS** - Разделение команд и запросов

## 🎯 Event Storming события

**События пользователей:**
- `UserViewedProduct` - Пользователь просмотрел товар
- `UserAddedToCart` - Пользователь добавил товар в корзину
- `UserPurchasedProduct` - Пользователь купил товар
- `UserSearchedProducts` - Пользователь искал товары

**События системы:**
- `RecommendationGenerated` - Сгенерированы рекомендации
- `RecommendationModelUpdated` - Обновлена модель рекомендаций
- `ModelTrainingStarted` - Началось обучение модели

## 📊 Демо-данные

Система автоматически загружает демо-данные при первом запуске:

**Пользователи:** `user-1`, `user-2`, `user-3`, `user-4`, `user-5`
**Товары:** `product-1` ... `product-10` (смартфоны, ноутбуки, аксессуары и т.д.)

## 🚀 Возможности масштабирования

- **Горизонтальное масштабирование** - каждый сервис можно масштабировать независимо
- **Отказоустойчивость** - выход из строя одного сервиса не останавливает систему
- **Zero-downtime развертывания** - модели ML обновляются без остановки сервиса
- **Event-driven** - слабая связанность сервисов через события

## 📈 Метрики качества

- **Latency:** < 100ms для получения рекомендаций
- **Availability:** 99.9% uptime
- **Scalability:** 10,000+ concurrent users
- **Cold start:** < 5 секунд для новых пользователей

## 👨‍💻 Автор

**Koshey** - Разработчик

Версия: **1.0.0**

Проект разработан в рамках обучения в **Университете ВИВТ** (Воронежский Институт Высоких Технологий)

---

## 📄 Лицензия

MIT License
